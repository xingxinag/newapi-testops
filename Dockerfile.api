FROM node:25-alpine

WORKDIR /app
COPY package.json ./
RUN npm install --omit=dev
COPY apps ./apps
COPY packages ./packages

ENV API_HOST=0.0.0.0
ENV API_PORT=8788
ENV DATA_DIR=/data
ENV ARTIFACT_LOCAL_DIR=/data/artifacts
EXPOSE 8788

CMD ["node", "apps/api/src/server.mjs"]
